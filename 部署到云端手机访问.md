---
# 行政工作台 - 手机版部署指南

## 直接回答你的三个问题

### 1. 手机版需要传到云端吗？
**是的。** 因为：
- 手机浏览器不能直接打开你电脑本地的 HTML 文件（即使能打开，数据也不同步）
- 传到云端后，你会得到一个网址（比如 `https://你的工作台.up.railway.app`）
- 手机和电脑都打开这个网址，登录同一个账号，数据就完全同步了

**如果你暂时不想上云**：也可以在自己电脑上跑服务，然后用「内网穿透」工具（如 ngrok、花生壳）给手机临时访问，但每次电脑关机网址就变了，不推荐长期用。

### 2. 账号密码登录还有吗？
**有，而且是强制的。** 部署后的网站一打开就是全屏登录页：
- 没有账号？点「没有账号？点击注册」先注册
- 登录成功后才能看到你记录的所有数据
- 退出登录后，页面立刻回到登录页，别人拿你手机也看不到内容

### 3. 数据会保留吗？退出后编辑的数据会不会没？
**不会丢！** 数据保存有三重保险：

| 保险层 | 说明 | 什么时候生效 |
|--------|------|-------------|
| 第一层：云端数据库 | 所有数据存在服务器数据库里 | 你每次编辑后立刻自动保存 |
| 第二层：JSON 自动备份 | 服务器每小时把数据库备份成 JSON 文件 | 后台自动跑，你不用管 |
| 第三层：手动备份下载 | 登录后点顶部「💾备份」按钮，把数据下载成 JSON 文件存到你手机/电脑 | **强烈建议每周手动下载一次** |

> 👉 只要你记得定期点「💾备份」下载 JSON 文件，哪怕云端炸了、平台跑路了，你的数据也在自己手里。点「♻️恢复」上传就能还原回来。

---

## 推荐部署方案（按推荐度排序）

### 方案 A：Railway（最推荐，5 分钟搞定，有持久化）
- **费用**：每月 $5 免费额度（你这个小系统一个月也就 $1-2，基本免费）
- **数据保留**：有持久化磁盘，数据不会因为重启而丢失
- **步骤**：

#### 第 1 步：把代码上传到 GitHub
1. 打开 [github.com](https://github.com)，注册/登录账号
2. 右上角 `+` → `New repository`
3. 仓库名填 `admin-workbench`，选 `Public`（公开的才能免费部署）
4. **不要**勾「Add a README」，其他保持默认
5. 点 `Create repository`
6. 在仓库页面，点「uploading an existing file」
7. 把你整个 `admin-workbench` 文件夹里的所有文件（除了 node_modules 和 data 文件夹）拖拽进去
8. 点页面底部的 `Commit changes` 上传完成

#### 第 2 步：在 Railway 部署
1. 打开 [railway.app](https://railway.app)，点右上角 `Login`，选 `Login with GitHub` 授权登录
2. 登录后点 `New Project` → `Deploy from GitHub repo`
3. 找到你刚才的 `admin-workbench` 仓库，点选中
4. Railway 会自动识别这是 Node.js 项目，开始部署（等 1-2 分钟）
5. 状态变绿后，点中间偏上的 `Settings` → 左边 `Variables`
6. 点 `New Variable`，添加一个环境变量：
   - 名字（Name）：`DB_PATH`
   - 值（Value）：`/app/data/workbench.sqlite`
7. 再添加一个：
   - 名字：`JWT_SECRET`
   - 值：随便填一串 30 位以上的字母数字（比如 `workbench-my-company-2026-abc123xyz`），**这个不能改，改了所有人都要重新登录**
8. 点左边 `Volumes` → `New Volume`
   - Mount Path 填：`/app/data`
   - Size 选 1GB 就够了
9. 点顶部 `Settings` → `Domains` → `Generate Domain`，Railway 会给你一个网址，比如 `https://xxx.up.railway.app`
10. **完成！** 手机/电脑浏览器打开这个网址就能用了

#### 第 3 步：第一次使用
1. 手机打开网址 → 点「没有账号？点击注册」
2. 注册你自己的账号密码（**记住！忘了目前无法找回，只能重新注册**）
3. 注册成功会自动登录，如果之前在本地浏览器有数据，会提示是否迁移
4. 建议马上点顶部的「💾备份」下载一份 JSON 存好

---

### 方案 B：Render（完全免费，但数据有丢失风险）
- **费用**：完全免费
- **缺点**：免费版空闲 15 分钟后会休眠，再次访问要等 10-30 秒。且数据库文件每天会清空一次。
- **数据保留**：靠「JSON 自动备份」+ 你手动下载备份。**必须每周点一次「💾备份」下载 JSON 存好。**
- **步骤**：
1. 代码上传 GitHub（同方案 A 第 1 步）
2. 打开 [render.com](https://render.com)，用 GitHub 登录
3. 点 `New +` → `Web Service`
4. 连接你的 `admin-workbench` 仓库
5. 配置：
   - Name: `admin-workbench`（随便写）
   - Runtime: `Node`
   - Build Command: `npm install`
   - Start Command: `node server.js`
   - Plan: 选 `Free`
6. 点 `Advanced` → `Add Environment Variable`：
   - Key: `JWT_SECRET`  Value: 随便写一串 30 位以上字母数字
7. 点 `Create Web Service`
8. 等 2-3 分钟部署完成，会给你一个 `onrender.com` 结尾的网址
9. 手机打开网址即可使用

> ⚠️ 用 Render 的话，**每周至少点一次顶部「💾备份」把 JSON 文件下载到自己手机/电脑**。如果某天登录发现数据没了，点「♻️恢复」上传你上次下载的 JSON 就能回来。

---

### 方案 C：自己的云服务器（最稳定，适合长期用）
如果你有阿里云、腾讯云等服务器（新用户一般有免费试用）：

```bash
# SSH 连上服务器后
sudo apt update
sudo apt install -y nodejs npm git
git clone https://github.com/你的用户名/admin-workbench.git
cd admin-workbench
npm install

# 安装 pm2 让服务后台运行
sudo npm install -g pm2
pm2 start server.js --name workbench
pm2 startup   # 开机自启
pm2 save

# 如果有域名/nginx，把 8080 端口反向代理即可
```

数据保存在服务器的 `/opt/render/project/src/data/`（或 DB_PATH 环境变量指定的路径），只要服务器不炸数据就不会丢。

---

## 手机上怎么用最方便

部署成功后，在手机上：

### iPhone (Safari)
1. Safari 打开你的网址
2. 登录账号密码
3. 点底部中间的「分享」按钮（方框上箭头）
4. 往下滑，点「添加到主屏幕」
5. 取个名字（比如「我的工作台」），点「添加」
6. 桌面就会出现一个 App 图标，以后点图标直接进，不用输网址

### Android (Chrome)
1. Chrome 打开你的网址
2. 登录账号密码
3. 点右上角三个点 → 「添加到主屏幕」或「安装应用」
4. 确认后桌面就出现图标了，以后直接点开用

### 数据安全提醒
1. **账号密码别忘**：目前无找回密码功能，忘了只能重新注册
2. **每周手动备份**：登录后点「💾备份」下载 JSON，存在手机相册/云盘里
3. **不要用公共 WiFi 登录敏感账号**：如果是 http 网址注意一下，https 就没问题（Railway/Render 默认都给 https）

---

## 常见问题

**Q: 我之前在本地（或 TRAE 里）有数据，部署后怎么转过去？**
A: 部署后先在 TRAE 的浏览器里打开新网址，用同一个账号登录，顶部会出现「📤迁移数据」按钮，点一下就把当前浏览器里存的旧数据上传到服务器了。之后手机登录同一账号就能看到。

**Q: 可以多人共用吗？**
A: 每个人注册自己的账号，数据互相隔离。目前没有多人共享功能。

**Q: 以后想加功能/改界面怎么办？**
A: 改完本地代码，重新上传到 GitHub，Railway/Render 会自动重新部署，1-2 分钟就生效。

**Q: 流量有限制吗？**
A: 你一个人用的话，免费方案的流量完全够。Railway 每月 $5 额度大概能跑几千次访问。
---
